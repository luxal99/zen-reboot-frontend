import { FindClientPhoneNumberPipe } from './find-client-phone-number.pipe';

describe('FindClientPhoneNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FindClientPhoneNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
