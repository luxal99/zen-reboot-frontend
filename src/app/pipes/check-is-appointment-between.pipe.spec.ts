import { CheckIsAppointmentBetweenPipe } from './check-is-appointment-between.pipe';

describe('CheckIsAppointmentBetweenPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckIsAppointmentBetweenPipe();
    expect(pipe).toBeTruthy();
  });
});
