import { ChechShiftPipe } from './chech-shift.pipe';

describe('ChechShiftPipe', () => {
  it('create an instance', () => {
    const pipe = new ChechShiftPipe();
    expect(pipe).toBeTruthy();
  });
});
