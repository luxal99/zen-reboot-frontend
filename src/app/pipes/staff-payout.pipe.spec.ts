import { StaffPayoutPipe } from './staff-payout.pipe';

describe('StaffPayoutPipe', () => {
  it('create an instance', () => {
    const pipe = new StaffPayoutPipe();
    expect(pipe).toBeTruthy();
  });
});
