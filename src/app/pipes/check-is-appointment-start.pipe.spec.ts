import { CheckIsAppointmentStartPipe } from './check-is-appointment-start.pipe';

describe('CheckIsAppointmentStartPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckIsAppointmentStartPipe();
    expect(pipe).toBeTruthy();
  });
});
